export const NUM_ROWS = 15;
export const NUM_COLUMNS = 10;
export const DEFAULT_BOARD_VALUE = { color: "black", active: false };
export const SQUARE_SIZE = 55;
export const GAME_BOARD_WIDTH = SQUARE_SIZE * NUM_COLUMNS;
export const GAME_BOARD_HEIGHT = SQUARE_SIZE * NUM_ROWS;
